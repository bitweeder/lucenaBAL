/*------------------------------------------------------------------------------

	Lucena Build Abstraction Library
	“lbalTest”
	Copyright © 2015-2019 Lucena
	All Rights Reserved

	This file is distributed under the University of Illinois Open Source
	License. See license/License.txt for details.

------------------------------------------------------------------------------*/


//	std
#include <iostream>


//	gtest
#include <gtest/gtest.h>


//	lbal
#include <lucenaBAL/lucenaBAL.hpp>


/*------------------------------------------------------------------------------
	Declarations
*/



#define LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING 0
#define LBAL_DIAGNOSTIC_LOG_ALL_STATUS 1
#define LBAL_DIAGNOSTIC_LOG_ALL_UNSUPPORTED 1

#if LBAL_DIAGNOSTIC_LOG_ALL_STATUS
	#define LBAL_DIAGNOSTIC_LOG_SUPPORTED(LBAL_DIAGNOSTIC_feature_) \
		std::cerr << LBAL_DIAGNOSTIC_feature_ " is supported" << std::endl;
#elif
	#define LBAL_DIAGNOSTIC_LOG_SUPPORTED(LBAL_DIAGNOSTIC_feature_)
#endif

#if LBAL_LOG_DIAGNOSTIC_ALL_STATUS || LBAL_DIAGNOSTIC_LOG_ALL_UNSUPPORTED
	#define LBAL_DIAGNOSTIC_LOG_UNSUPPORTED(LBAL_DIAGNOSTIC_feature_) \
		std::cerr << LBAL_DIAGNOSTIC_feature_ " is UNSUPPORTED" << std::endl;
#elif
	#define LBAL_DIAGNOSTIC_LOG_UNSUPPORTED(LBAL_DIAGNOSTIC_feature_)
#endif

//	__FIXME__ This only works in C++17 or later, being a holdover from before
//	the more inclusive rewrite.
#define LBAL_DIAGNOSTIC_TEST(LBAL_DIAGNOSTIC_feature_) \
	do { \
		if constexpr (0 == LBAL_DIAGNOSTIC_feature_) { \
			LBAL_DIAGNOSTIC_LOG_UNSUPPORTED (#LBAL_DIAGNOSTIC_feature_) \
		} else { \
			LBAL_DIAGNOSTIC_LOG_SUPPORTED (#LBAL_DIAGNOSTIC_feature_) \
		} \
	} while (false)


using namespace LBAL_;


/*------------------------------------------------------------------------------
*/

GTEST_TEST (lbalTest, Validation)
{
	/*	Report the results of the various feature tests. In addition to
		runtime reporting, there is also compile-time reporting; depending on
		your IDE, you may not even need to explicitly compile to see the test
		results.

		__SEEME__ Note that we can’t use preprocessor or template magic when
		displaying compile-time messages; if #warning allowed macro
		substitution, we’d be in luck, but unfortunately, that feature is an
		MSVC extension, only, so gratuitous boilerplate is needed.
	*/

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP98_EXCEPTIONS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP98_EXCEPTIONS
		#warning "LBAL_CPP98_EXCEPTIONS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP98_RTTI);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP98_RTTI
		#warning "LBAL_CPP98_RTTI is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_ALIAS_TEMPLATES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_ALIAS_TEMPLATES
		#warning "LBAL_CPP11_ALIAS_TEMPLATES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_ATTRIBUTE_CARRIES_DEPENDENCY);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_ATTRIBUTE_CARRIES_DEPENDENCY
		#warning "LBAL_CPP11_ATTRIBUTE_CARRIES_DEPENDENCY is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_ATTRIBUTE_NORETURN);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_ATTRIBUTE_NORETURN
		#warning "LBAL_CPP11_ATTRIBUTE_NORETURN is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_ATTRIBUTES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_ATTRIBUTES
		#warning "LBAL_CPP11_ATTRIBUTES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_CONSTEXPR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_CONSTEXPR
		#warning "LBAL_CPP11_CONSTEXPR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_DECLTYPE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_DECLTYPE
		#warning "LBAL_CPP11_DECLTYPE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_DELEGATING_CONSTRUCTORS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_DELEGATING_CONSTRUCTORS
		#warning "LBAL_CPP11_DELEGATING_CONSTRUCTORS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_INHERITING_CONSTRUCTORS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_INHERITING_CONSTRUCTORS
		#warning "LBAL_CPP11_INHERITING_CONSTRUCTORS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_INITIALIZER_LISTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_INITIALIZER_LISTS
		#warning "LBAL_CPP11_INITIALIZER_LISTS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_LAMBDAS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_LAMBDAS
		#warning "LBAL_CPP11_LAMBDAS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_MINIMAL_GARBAGE_COLLECTION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_MINIMAL_GARBAGE_COLLECTION
		#warning "LBAL_CPP11_MINIMAL_GARBAGE_COLLECTION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_NSDMI);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_NSDMI
		#warning "LBAL_CPP11_NSDMI is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_RANGE_BASED_FOR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_RANGE_BASED_FOR
		#warning "LBAL_CPP11_RANGE_BASED_FOR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_RAW_STRINGS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_RAW_STRINGS
		#warning "LBAL_CPP11_RAW_STRINGS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_REF_QUALIFIERS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_REF_QUALIFIERS
		#warning "LBAL_CPP11_REF_QUALIFIERS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_RVALUE_REFERENCES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_RVALUE_REFERENCES
		#warning "LBAL_CPP11_RVALUE_REFERENCES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_STATIC_ASSERT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_STATIC_ASSERT
		#warning "LBAL_CPP11_STATIC_ASSERT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_THREADSAFE_STATIC_INIT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_THREADSAFE_STATIC_INIT
		#warning "LBAL_CPP11_THREADSAFE_STATIC_INIT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_UNICODE_CHARACTERS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_UNICODE_CHARACTERS
		#warning "LBAL_CPP11_UNICODE_CHARACTERS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_UNICODE_LITERALS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_UNICODE_LITERALS
		#warning "LBAL_CPP11_UNICODE_LITERALS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_USER_DEFINED_LITERALS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_USER_DEFINED_LITERALS
		#warning "LBAL_CPP11_USER_DEFINED_LITERALS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP11_VARIADIC_TEMPLATES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP11_VARIADIC_TEMPLATES
		#warning "LBAL_CPP11_VARIADIC_TEMPLATES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_AGGREGATE_NSDMI);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_AGGREGATE_NSDMI
		#warning "LBAL_CPP14_AGGREGATE_NSDMI is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_ATTRIBUTE_DEPRECATED);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_ATTRIBUTE_DEPRECATED
		#warning "LBAL_CPP14_ATTRIBUTE_DEPRECATED is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_BINARY_LITERALS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_BINARY_LITERALS
		#warning "LBAL_CPP14_BINARY_LITERALS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_CONSTEXPR_RELAXED_CONSTRAINTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_CONSTEXPR_RELAXED_CONSTRAINTS
		#warning "LBAL_CPP14_CONSTEXPR_RELAXED_CONSTRAINTS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_DECLTYPE_AUTO);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_DECLTYPE_AUTO
		#warning "LBAL_CPP14_DECLTYPE_AUTO is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_GENERIC_LAMBDAS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_GENERIC_LAMBDAS
		#warning "LBAL_CPP14_GENERIC_LAMBDAS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_INIT_CAPTURES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_INIT_CAPTURES
		#warning "LBAL_CPP14_INIT_CAPTURES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_RETURN_TYPE_DEDUCTION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_RETURN_TYPE_DEDUCTION
		#warning "LBAL_CPP14_RETURN_TYPE_DEDUCTION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_SIZED_DEALLOCATION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_SIZED_DEALLOCATION
		#warning "LBAL_CPP14_SIZED_DEALLOCATION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP14_VARIABLE_TEMPLATES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP14_VARIABLE_TEMPLATES
		#warning "LBAL_CPP14_VARIABLE_TEMPLATES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_AGGREGATE_BASES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_AGGREGATE_BASES
		#warning "LBAL_CPP17_AGGREGATE_BASES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_ALIGNED_NEW);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_ALIGNED_NEW
		#warning "LBAL_CPP17_ALIGNED_NEW is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_ATTRIBUTE_FALLTHROUGH);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_ATTRIBUTE_FALLTHROUGH
		#warning "LBAL_CPP17_ATTRIBUTE_FALLTHROUGH is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_ATTRIBUTE_MAYBE_UNUSED);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_ATTRIBUTE_MAYBE_UNUSED
		#warning "LBAL_CPP17_ATTRIBUTE_MAYBE_UNUSED is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_ATTRIBUTE_NODISCARD);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_ATTRIBUTE_NODISCARD
		#warning "LBAL_CPP17_ATTRIBUTE_NODISCARD is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_CAPTURE_STAR_THIS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_CAPTURE_STAR_THIS
		#warning "LBAL_CPP17_CAPTURE_STAR_THIS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_CONSTEXPR_LAMBDA);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_CONSTEXPR_LAMBDA
		#warning "LBAL_CPP17_CONSTEXPR_LAMBDA is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_DEDUCTION_GUIDES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_DEDUCTION_GUIDES
		#warning "LBAL_CPP17_DEDUCTION_GUIDES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_DEDUCTION_GUIDES_DR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_DEDUCTION_GUIDES_DR
		#warning "LBAL_CPP17_DEDUCTION_GUIDES_DR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_DEDUCTION_GUIDES_NB);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_DEDUCTION_GUIDES_NB
		#warning "LBAL_CPP17_DEDUCTION_GUIDES_NB is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_ENUMERATOR_ATTRIBUTES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_ENUMERATOR_ATTRIBUTES
		#warning "LBAL_CPP17_ENUMERATOR_ATTRIBUTES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_FOLD_EXPRESSIONS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_FOLD_EXPRESSIONS
		#warning "LBAL_CPP17_FOLD_EXPRESSIONS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_FOLD_EXPRESSIONS_REVISED);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_FOLD_EXPRESSIONS_REVISED
		#warning "LBAL_CPP17_FOLD_EXPRESSIONS_REVISED is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_GUARANTEED_COPY_ELISION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_GUARANTEED_COPY_ELISION
		#warning "LBAL_CPP17_GUARANTEED_COPY_ELISION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_HEX_FLOAT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_HEX_FLOAT
		#warning "LBAL_CPP17_HEX_FLOAT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_INHERITING_CONSTRUCTORS_REVISED);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_INHERITING_CONSTRUCTORS_REVISED
		#warning "LBAL_CPP17_INHERITING_CONSTRUCTORS_REVISED is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_INLINE_VARIABLES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_INLINE_VARIABLES
		#warning "LBAL_CPP17_INLINE_VARIABLES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_NAMESPACE_ATTRIBUTES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_NAMESPACE_ATTRIBUTES
		#warning "LBAL_CPP17_NAMESPACE_ATTRIBUTES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_NOEXCEPT_FUNCTION_TYPE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_NOEXCEPT_FUNCTION_TYPE
		#warning "LBAL_CPP17_NOEXCEPT_FUNCTION_TYPE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_NONTYPE_TEMPLATE_ARGS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_NONTYPE_TEMPLATE_ARGS
		#warning "LBAL_CPP17_NONTYPE_TEMPLATE_ARGS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_NONTYPE_TEMPLATE_PARAMETER_AUTO);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_NONTYPE_TEMPLATE_PARAMETER_AUTO
		#warning "LBAL_CPP17_NONTYPE_TEMPLATE_PARAMETER_AUTO is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_RANGE_BASED_FOR_GENERALIZED);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_RANGE_BASED_FOR_GENERALIZED
		#warning "LBAL_CPP17_RANGE_BASED_FOR_GENERALIZED is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_STATIC_ASSERT_NO_MESSAGE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_STATIC_ASSERT_NO_MESSAGE
		#warning "LBAL_CPP17_STATIC_ASSERT_NO_MESSAGE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_STRUCTURED_BINDINGS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_STRUCTURED_BINDINGS
		#warning "LBAL_CPP17_STRUCTURED_BINDINGS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_TEMPLATE_TEMPLATE_ARGS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_TEMPLATE_TEMPLATE_ARGS
		#warning "LBAL_CPP17_TEMPLATE_TEMPLATE_ARGS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP17_VARIADIC_USING);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP17_VARIADIC_USING
		#warning "LBAL_CPP17_VARIADIC_USING is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_AGGREGATE_PAREN_INIT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_AGGREGATE_PAREN_INIT
		#warning "LBAL_CPP2A_AGGREGATE_PAREN_INIT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CHAR8_T);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CHAR8_T
		#warning "LBAL_CPP2A_CHAR8_T is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_ALLOW_LAMBDA_CAPTURE_EQUALS_THIS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_ALLOW_LAMBDA_CAPTURE_EQUALS_THIS
		#warning "LBAL_CPP2A_ALLOW_LAMBDA_CAPTURE_EQUALS_THIS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_ATTRIBUTE_LIKELY);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_ATTRIBUTE_LIKELY
		#warning "LBAL_CPP2A_ATTRIBUTE_LIKELY is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_ATTRIBUTES_LIKELY_AND_UNLIKELY);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_ATTRIBUTES_LIKELY_AND_UNLIKELY
		#warning "LBAL_CPP2A_ATTRIBUTES_LIKELY_AND_UNLIKELY is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_ATTRIBUTE_NO_UNIQUE_ADDRESS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_ATTRIBUTE_NO_UNIQUE_ADDRESS
		#warning "LBAL_CPP2A_ATTRIBUTE_NO_UNIQUE_ADDRESS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_ATTRIBUTE_NODISCARD_EXPANDED);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_ATTRIBUTE_NODISCARD_EXPANDED
		#warning "LBAL_CPP2A_ATTRIBUTE_NODISCARD_EXPANDED is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_ATTRIBUTE_UNLIKELY);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_ATTRIBUTE_UNLIKELY
		#warning "LBAL_CPP2A_ATTRIBUTE_UNLIKELY is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONCEPTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONCEPTS
		#warning "LBAL_CPP2A_CONCEPTS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONCEPTS_RESPECIFY_RETURN_TYPE_REQUIREMENTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONCEPTS_RESPECIFY_RETURN_TYPE_REQUIREMENTS
		#warning "LBAL_CPP2A_CONCEPTS_RESPECIFY_RETURN_TYPE_REQUIREMENTS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONCEPTS_REFINE_RETURN_TYPE_REQUIREMENTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONCEPTS_REFINE_RETURN_TYPE_REQUIREMENTS
		#warning "LBAL_CPP2A_CONCEPTS_REFINE_RETURN_TYPE_REQUIREMENTS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONDITIONAL_EXPLICIT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONDITIONAL_EXPLICIT
		#warning "LBAL_CPP2A_CONDITIONAL_EXPLICIT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONST_REF_QUALIFIED_POINTERS_TO_MEMBERS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONST_REF_QUALIFIED_POINTERS_TO_MEMBERS
		#warning "LBAL_CPP2A_CONST_REF_QUALIFIED_POINTERS_TO_MEMBERS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEVAL);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEVAL
		#warning "LBAL_CPP2A_CONSTEVAL is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_DYNAMIC_ALLOC);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_DYNAMIC_ALLOC
		#warning "LBAL_CPP2A_CONSTEXPR_DYNAMIC_ALLOC is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_DYNAMIC_POLYMORPHISM);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_DYNAMIC_POLYMORPHISM
		#warning "LBAL_CPP2A_CONSTEXPR_DYNAMIC_POLYMORPHISM is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_IN_DECLTYPE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_IN_DECLTYPE
		#warning "LBAL_CPP2A_CONSTEXPR_IN_DECLTYPE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_INTRINSICS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_INTRINSICS
		#warning "LBAL_CPP2A_CONSTEXPR_INTRINSICS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_TRIVIAL_DEFAULT_INITIALIZATION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_TRIVIAL_DEFAULT_INITIALIZATION
		#warning "LBAL_CPP2A_CONSTEXPR_TRIVIAL_DEFAULT_INITIALIZATION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_TRY_CATCH);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_TRY_CATCH
		#warning "LBAL_CPP2A_CONSTEXPR_TRY_CATCH is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_UNION_ALTERATION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_UNION_ALTERATION
		#warning "LBAL_CPP2A_CONSTEXPR_UNION_ALTERATION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTEXPR_VIRTUAL_FUNCTION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTEXPR_VIRTUAL_FUNCTION
		#warning "LBAL_CPP2A_CONSTEXPR_VIRTUAL_FUNCTION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_CONSTINIT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_CONSTINIT
		#warning "LBAL_CPP2A_CONSTINIT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_COROUTINES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_COROUTINES
		#warning "LBAL_CPP2A_COROUTINES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_DEDUCTION_GUIDES_FOR_AGGREGATES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_DEDUCTION_GUIDES_FOR_AGGREGATES
		#warning "LBAL_CPP2A_DEDUCTION_GUIDES_FOR_AGGREGATES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_DEFAULT_CONSTRUCTIBLE_AND_ASSIGNABLE_STATELESS_LAMBDAS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_DEFAULT_CONSTRUCTIBLE_AND_ASSIGNABLE_STATELESS_LAMBDAS
		#warning "LBAL_CPP2A_DEFAULT_CONSTRUCTIBLE_AND_ASSIGNABLE_STATELESS_LAMBDAS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_DEFAULT_MEMBER_INITIALIZERS_FOR_BIT_FIELDS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_DEFAULT_MEMBER_INITIALIZERS_FOR_BIT_FIELDS
		#warning "LBAL_CPP2A_DEFAULT_MEMBER_INITIALIZERS_FOR_BIT_FIELDS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_DESIGNATED_INITIALIZERS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_DESIGNATED_INITIALIZERS
		#warning "LBAL_CPP2A_DESIGNATED_INITIALIZERS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_DESTROYING_DELETE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_DESTROYING_DELETE
		#warning "LBAL_CPP2A_DESTROYING_DELETE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_GENERIC_LAMBDAS_TEMPLATE_PARAMETER_LIST);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_GENERIC_LAMBDAS_TEMPLATE_PARAMETER_LIST
		#warning "LBAL_CPP2A_GENERIC_LAMBDAS_TEMPLATE_PARAMETER_LIST is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_INIT_CAPTURES_PACK_EXPANSION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_INIT_CAPTURES_PACK_EXPANSION
		#warning "LBAL_CPP2A_INIT_CAPTURES_PACK_EXPANSION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_INIT_STATEMENTS_FOR_RANGE_BASED_FOR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_INIT_STATEMENTS_FOR_RANGE_BASED_FOR
		#warning "LBAL_CPP2A_INIT_STATEMENTS_FOR_RANGE_BASED_FOR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_INITIALIZER_LIST_CONSTRUCTORS_IN_CLASS_TEMPLATE_ARGUMENT_DEDUCTION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_INITIALIZER_LIST_CONSTRUCTORS_IN_CLASS_TEMPLATE_ARGUMENT_DEDUCTION
		#warning "LBAL_CPP2A_INITIALIZER_LIST_CONSTRUCTORS_IN_CLASS_TEMPLATE_ARGUMENT_DEDUCTION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_INTEGRATING_OUR_FEATURE_TEST_MACROS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_INTEGRATING_OUR_FEATURE_TEST_MACROS
		#warning "LBAL_CPP2A_INTEGRATING_OUR_FEATURE_TEST_MACROS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_MODULES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_MODULES
		#warning "LBAL_CPP2A_MODULES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_NONTYPE_TEMPLATE_ARGS_FIXES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_NONTYPE_TEMPLATE_ARGS_FIXES
		#warning "LBAL_CPP2A_NONTYPE_TEMPLATE_ARGS_FIXES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_NONTYPE_TEMPLATE_PARAMETER_CLASS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_NONTYPE_TEMPLATE_PARAMETER_CLASS
		#warning "LBAL_CPP2A_NONTYPE_TEMPLATE_PARAMETER_CLASS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR
		#warning "LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR_EQUALITY_FIX);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR_EQUALITY_FIX
		#warning "LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR_EQUALITY_FIX is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR_TUNEUP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR_TUNEUP
		#warning "LBAL_CPP2A_THREE_WAY_COMPARISON_OPERATOR_TUNEUP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_TYPENAME_OPTIONAL);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_TYPENAME_OPTIONAL
		#warning "LBAL_CPP2A_TYPENAME_OPTIONAL is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_USING_ENUM);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_USING_ENUM
		#warning "LBAL_CPP2A_USING_ENUM is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPP2A_VA_OPT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPP2A_VA_OPT
		#warning "LBAL_CPP2A_VA_OPT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_C99_PREPROCESSOR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_C99_PREPROCESSOR
		#warning "LBAL_C99_PREPROCESSOR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ADDRESSOF_CONSTEXPR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ADDRESSOF_CONSTEXPR
		#warning "LBAL_LIBCPP17_ADDRESSOF_CONSTEXPR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP14_CHRONO_UDLS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP14_CHRONO_UDLS
		#warning "LBAL_LIBCPP14_CHRONO_UDLS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP14_COMPLEX_UDLS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP14_COMPLEX_UDLS
		#warning "LBAL_LIBCPP14_COMPLEX_UDLS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP14_EXCHANGE_FUNCTION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP14_EXCHANGE_FUNCTION
		#warning "LBAL_LIBCPP14_EXCHANGE_FUNCTION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP14_GENERIC_ASSOCIATIVE_LOOKUP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP14_GENERIC_ASSOCIATIVE_LOOKUP
		#warning "LBAL_LIBCPP14_GENERIC_ASSOCIATIVE_LOOKUP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ALLOCATOR_TRAITS_IS_ALWAYS_EQUAL);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ALLOCATOR_TRAITS_IS_ALWAYS_EQUAL
		#warning "LBAL_LIBCPP17_ALLOCATOR_TRAITS_IS_ALWAYS_EQUAL is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ANY);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ANY
		#warning "LBAL_LIBCPP17_ANY is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ANY_INTERFACE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ANY_INTERFACE
		#warning "LBAL_LIBCPP17_ANY_INTERFACE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_APPLY);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_APPLY
		#warning "LBAL_LIBCPP17_APPLY is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ARRAY_CONSTEXPR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ARRAY_CONSTEXPR
		#warning "LBAL_LIBCPP17_ARRAY_CONSTEXPR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_AS_CONST);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_AS_CONST
		#warning "LBAL_LIBCPP17_AS_CONST is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ATOMIC_IS_ALWAYS_LOCK_FREE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ATOMIC_IS_ALWAYS_LOCK_FREE
		#warning "LBAL_LIBCPP17_ATOMIC_IS_ALWAYS_LOCK_FREE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_BOOL_CONSTANT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_BOOL_CONSTANT
		#warning "LBAL_LIBCPP17_BOOL_CONSTANT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_BOYER_MOORE_SEARCHER);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_BOYER_MOORE_SEARCHER
		#warning "LBAL_LIBCPP17_BOYER_MOORE_SEARCHER is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_BYTE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_BYTE
		#warning "LBAL_LIBCPP17_BYTE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_CHRONO);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_CHRONO
		#warning "LBAL_LIBCPP17_CHRONO is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_CHRONO_CONSTEXPR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_CHRONO_CONSTEXPR
		#warning "LBAL_LIBCPP17_CHRONO_CONSTEXPR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_CLAMP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_CLAMP
		#warning "LBAL_LIBCPP17_CLAMP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_EXECUTION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_EXECUTION
		#warning "LBAL_LIBCPP17_EXECUTION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ENABLE_SHARED_FROM_THIS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ENABLE_SHARED_FROM_THIS
		#warning "LBAL_LIBCPP17_ENABLE_SHARED_FROM_THIS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_ENABLE_OWNER_LESS_VOID);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_ENABLE_OWNER_LESS_VOID
		#warning "LBAL_LIBCPP17_ENABLE_OWNER_LESS_VOID is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_FILESYSTEM);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_FILESYSTEM
		#warning "LBAL_LIBCPP17_FILESYSTEM is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_FILESYSTEM_PATHS_EXPANDED);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_FILESYSTEM_PATHS_EXPANDED
		#warning "LBAL_LIBCPP17_FILESYSTEM_PATHS_EXPANDED is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_FILESYSTEM_DIRECTORY_ENTRY_CACHING);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_FILESYSTEM_DIRECTORY_ENTRY_CACHING
		#warning "LBAL_LIBCPP17_FILESYSTEM_DIRECTORY_ENTRY_CACHING is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_FILESYSTEM_EXP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_FILESYSTEM_EXP
		#warning "LBAL_LIBCPP17_FILESYSTEM_EXP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_GCD_LCM);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_GCD_LCM
		#warning "LBAL_LIBCPP17_GCD_LCM is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_HARDWARE_INTERFERENCE_SIZE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_HARDWARE_INTERFERENCE_SIZE
		#warning "LBAL_LIBCPP17_HARDWARE_INTERFERENCE_SIZE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_HAS_UNIQUE_OBJECT_REPRESENTATIONS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_HAS_UNIQUE_OBJECT_REPRESENTATIONS
		#warning "LBAL_LIBCPP17_HAS_UNIQUE_OBJECT_REPRESENTATIONS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_HYPOT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_HYPOT
		#warning "LBAL_LIBCPP17_HYPOT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_LAUNDER);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_LAUNDER
		#warning "LBAL_LIBCPP17_LAUNDER is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_OPTIONAL);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_OPTIONAL
		#warning "LBAL_LIBCPP17_OPTIONAL is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_PARALLEL_ALGORITHM);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_PARALLEL_ALGORITHM
		#warning "LBAL_LIBCPP17_PARALLEL_ALGORITHM is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_SPLICING_MAPS_AND_SETS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_SPLICING_MAPS_AND_SETS
		#warning "LBAL_LIBCPP17_SPLICING_MAPS_AND_SETS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_STANDARDIZATION_OF_PARALLELISM_TS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_STANDARDIZATION_OF_PARALLELISM_TS
		#warning "LBAL_LIBCPP17_STANDARDIZATION_OF_PARALLELISM_TS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_TO_CHARS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_TO_CHARS
		#warning "LBAL_LIBCPP17_TO_CHARS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_TO_CHARS_FP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_TO_CHARS_FP
		#warning "LBAL_LIBCPP17_TO_CHARS_FP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_TO_CHARS_INTEGER);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_TO_CHARS_INTEGER
		#warning "LBAL_LIBCPP17_TO_CHARS_INTEGER is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP17_VARIANT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP17_VARIANT
		#warning "LBAL_LIBCPP17_VARIANT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ARRAY_CONSTEXPR_ITERS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ARRAY_CONSTEXPR_ITERS
		#warning "LBAL_LIBCPP2A_ARRAY_CONSTEXPR_ITERS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ATOMIC_FLAG_TEST);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ATOMIC_FLAG_TEST
		#warning "LBAL_LIBCPP2A_ATOMIC_FLAG_TEST is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ATOMIC_LOCK_FREE_TYPE_ALIASES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ATOMIC_LOCK_FREE_TYPE_ALIASES
		#warning "LBAL_LIBCPP2A_ATOMIC_LOCK_FREE_TYPE_ALIASES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ATOMIC_REF);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ATOMIC_REF
		#warning "LBAL_LIBCPP2A_ATOMIC_REF is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ATOMIC_WAIT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ATOMIC_WAIT
		#warning "LBAL_LIBCPP2A_ATOMIC_WAIT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_BARRIER);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_BARRIER
		#warning "LBAL_LIBCPP2A_BARRIER is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_BIND_FRONT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_BIND_FRONT
		#warning "LBAL_LIBCPP2A_BIND_FRONT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_BIND_FRONT_NO_UNWRAP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_BIND_FRONT_NO_UNWRAP
		#warning "LBAL_LIBCPP2A_BIND_FRONT_NO_UNWRAP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_BIT_CAST);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_BIT_CAST
		#warning "LBAL_LIBCPP2A_BIT_CAST is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_BITOPS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_BITOPS
		#warning "LBAL_LIBCPP2A_BITOPS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_BOUNDED_ARRAY_TRAITS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_BOUNDED_ARRAY_TRAITS
		#warning "LBAL_LIBCPP2A_BOUNDED_ARRAY_TRAITS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CALENDAR_AND_TIMEZONE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CALENDAR_AND_TIMEZONE
		#warning "LBAL_LIBCPP2A_CALENDAR_AND_TIMEZONE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CHAR8_T);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CHAR8_T
		#warning "LBAL_LIBCPP2A_CHAR8_T is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CHAR8_T_COMPAT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CHAR8_T_COMPAT
		#warning "LBAL_LIBCPP2A_CHAR8_T_COMPAT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CHRONO_FIXES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CHRONO_FIXES
		#warning "LBAL_LIBCPP2A_CHRONO_FIXES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONCEPTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONCEPTS
		#warning "LBAL_LIBCPP2A_CONCEPTS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONCEPTS_EXP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONCEPTS_EXP
		#warning "LBAL_LIBCPP2A_CONCEPTS_EXP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONSTEXPR);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONSTEXPR
		#warning "LBAL_LIBCPP2A_CONSTEXPR is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS
		#warning "LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS_SWAP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS_SWAP
		#warning "LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS_SWAP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS_SWAP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS_SWAP
		#warning "LBAL_LIBCPP2A_CONSTEXPR_ALGORITHMS_SWAP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONSTEXPR_DYNAMIC_ALLOC);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONSTEXPR_DYNAMIC_ALLOC
		#warning "LBAL_LIBCPP2A_CONSTEXPR_DYNAMIC_ALLOC is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONSTEXPR_INVOKE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONSTEXPR_INVOKE
		#warning "LBAL_LIBCPP2A_CONSTEXPR_INVOKE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_CONSTEXPR_STRING);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_CONSTEXPR_STRING
		#warning "LBAL_LIBCPP2A_CONSTEXPR_STRING is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_COROUTINES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_COROUTINES
		#warning "LBAL_LIBCPP2A_COROUTINES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_COROUTINES_EXP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_COROUTINES_EXP
		#warning "LBAL_LIBCPP2A_COROUTINES_EXP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_DESTROYING_DELETE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_DESTROYING_DELETE
		#warning "LBAL_LIBCPP2A_DESTROYING_DELETE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ENDIAN);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ENDIAN
		#warning "LBAL_LIBCPP2A_ENDIAN is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ENDIAN_BIT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ENDIAN_BIT
		#warning "LBAL_LIBCPP2A_ENDIAN_BIT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_ERASE_IF);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_ERASE_IF
		#warning "LBAL_LIBCPP2A_ERASE_IF is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_EXTENDING_MAKE_SHARED_TO_SUPPORT_ARRAYS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_EXTENDING_MAKE_SHARED_TO_SUPPORT_ARRAYS
		#warning "LBAL_LIBCPP2A_EXTENDING_MAKE_SHARED_TO_SUPPORT_ARRAYS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_FLOATING_POINT_ATOMIC);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_FLOATING_POINT_ATOMIC
		#warning "LBAL_LIBCPP2A_FLOATING_POINT_ATOMIC is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_FORMAT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_FORMAT
		#warning "LBAL_LIBCPP2A_FORMAT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_GENERIC_UNORDERED_HASH_LOOKUP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_GENERIC_UNORDERED_HASH_LOOKUP
		#warning "LBAL_LIBCPP2A_GENERIC_UNORDERED_HASH_LOOKUP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_GENERIC_UNORDERED_LOOKUP);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_GENERIC_UNORDERED_LOOKUP
		#warning "LBAL_LIBCPP2A_GENERIC_UNORDERED_LOOKUP is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_LIST_REMOVE_RETURN_TYPE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_LIST_REMOVE_RETURN_TYPE
		#warning "LBAL_LIBCPP2A_LIST_REMOVE_RETURN_TYPE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_MORE_CONSTEXPR_FOR_COMPLEX);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_MORE_CONSTEXPR_FOR_COMPLEX
		#warning "LBAL_LIBCPP2A_MORE_CONSTEXPR_FOR_COMPLEX is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_SPAN);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_SPAN
		#warning "LBAL_LIBCPP2A_SPAN is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_STD_REMOVE_CVREF);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_STD_REMOVE_CVREF
		#warning "LBAL_LIBCPP2A_STD_REMOVE_CVREF is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_STRING_PREFIX_AND_SUFFIX_CHECKING);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_STRING_PREFIX_AND_SUFFIX_CHECKING
		#warning "LBAL_LIBCPP2A_STRING_PREFIX_AND_SUFFIX_CHECKING is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_SYNCHRONIZED_BUFFERED_OSTREAM);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_SYNCHRONIZED_BUFFERED_OSTREAM
		#warning "LBAL_LIBCPP2A_SYNCHRONIZED_BUFFERED_OSTREAM is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_THREE_WAY_COMPARISON_OPERATOR_SUPPORT_COMPARE);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_THREE_WAY_COMPARISON_OPERATOR_SUPPORT_COMPARE
		#warning "LBAL_LIBCPP2A_THREE_WAY_COMPARISON_OPERATOR_SUPPORT_COMPARE is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_TYPE_IDENTITY);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_TYPE_IDENTITY
		#warning "LBAL_LIBCPP2A_TYPE_IDENTITY is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_UTILITY_TO_CONVERT_A_POINTER_TO_A_RAW_POINTER);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_UTILITY_TO_CONVERT_A_POINTER_TO_A_RAW_POINTER
		#warning "LBAL_LIBCPP2A_UTILITY_TO_CONVERT_A_POINTER_TO_A_RAW_POINTER is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_LIBCPP2A_VERSION);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_LIBCPP2A_VERSION
		#warning "LBAL_LIBCPP2A_VERSION is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPPTS_ATTRIBUTE_ASSERT);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPPTS_ATTRIBUTE_ASSERT
		#warning "LBAL_CPPTS_ATTRIBUTE_ASSERT is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPPTS_ATTRIBUTE_ENSURES);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPPTS_ATTRIBUTE_ENSURES
		#warning "LBAL_CPPTS_ATTRIBUTE_ENSURES is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPPTS_ATTRIBUTE_EXPECTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPPTS_ATTRIBUTE_EXPECTS
		#warning "LBAL_CPPTS_ATTRIBUTE_EXPECTS is UNSUPPORTED"
	#endif

	LBAL_DIAGNOSTIC_TEST(LBAL_CPPTS_CONTRACTS);
	#if LBAL_DIAGNOSTIC_COMPILE_TIME_REPORTING && !LBAL_CPPTS_CONTRACTS
		#warning "LBAL_CPPTS_CONTRACTS is UNSUPPORTED"
	#endif

	EXPECT_TRUE (true);
}
